---
tags:
  - Linux_Test
related:
  - "[[00_Linux_Challenge_DashBoard]]"
  - "[[Linux_Grep]]"
source: programmers
difficulty:
  - Medium
---
## 문제 분석 & 파이프라인 설계 (Shell logic)

> `"that"`이라는 단어가 **포함된 줄은 제거**
>  **대소문자를 구분하지 않음**
>   즉, `"that"`, `"That"`, `"THAT"` 모두 제거 대상

1. **Input/Output 데이터 형태:**
    - **Input**: 텍스트 파일
    - **Output**: `"that"`이라는 단어가 **포함되지 않은 줄만 출력**
2. **핵심 명령어 & 로직 (Pipeline):**

3. **사용할 주요 옵션 (Flags):**
	- `-v` → **포함된 줄 제외 (반대 선택)**
	- `-i` → 대소문자 무시 (`that`, `That`, `THAT`)
	- `-w` → **단어 단위로 정확히 매칭**
---
## ✅ 정답 스크립트 (Solution)

```bash
#!/bin/bash

grep -ivw 'that'
```

---
## 오답 노트 & 배운 점 (Retrospective)

- **내가 실수한 부분 (Syntax/Spacing):**

-  **새로 알게 된 명령어/옵션:**


---
## 더 나은 풀이 (One-liner / Efficiency)

```bash
# 숏코딩 / 효율적인 한 줄 명령어
```
