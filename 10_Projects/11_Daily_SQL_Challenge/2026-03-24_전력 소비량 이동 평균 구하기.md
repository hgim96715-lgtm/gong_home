---
tags:
  - SQL_TEST
related:
  - "[[00_SQL_Challenge_DashBoard]]"
  - "[[SQL_Window_Functions#⑤ Windowing 절 — 범위 세밀 지정]]"
source: solvesql
difficulty:
  - Lv.4
---
##  해결 전략 (Code Before Think)

> 2017년 1월 1일 0시부터 2017년 2월 1일 0시까지 10분 단위로 3개 발전소 전력 소비량의 1시간 범위 단순 이동 평균을 계산하는 쿼리를 작성해주세요. 평균을 내기 위한 데이터의 범위가 1시간보다 작은 경우 해당 범위에 포함되는 측정 값만 평균 계산에 포함시켜주세요.

1. **타겟 데이터:** (어떤 테이블에서 무엇을 뽑아야 하는가?)
   - `power_consumptions` 테이블
	   - `measured_at`  측정 시작 시각 (10분 동안 측정)
	   - `zone_quads` Quads 지역 전력 소비량 (kWh)
	   - ` zone_smir` Smir 지역 전력 소비량 (kWh)
	   - `zone_boussafou` Boussafou 지역 전력 소비량 (kWh)
1. **조건 분석:**
   - 
3. **사용할 문법:**
   - 
---
## 정답 쿼리 (Solution)

```sql
-- 여기에 작성한 쿼리를 붙여넣으세요.
SELECT
    measured_at + INTERVAL '10 minute' AS end_at,
    ROUND(AVG(zone_quads) OVER(ORDER BY measured_at ROWS BETWEEN 5 PRECEDING AND CURRENT ROW), 2) AS zone_quads,
    ROUND(AVG(zone_smir) OVER(ORDER BY measured_at ROWS BETWEEN 5 PRECEDING AND CURRENT ROW), 2) AS zone_smir,
    ROUND(AVG(zone_boussafou) OVER(ORDER BY measured_at ROWS BETWEEN 5 PRECEDING AND CURRENT ROW), 2) AS zone_boussafou
FROM power_consumptions
WHERE measured_at >= '2017-01-01' AND measured_at < '2017-02-01'
ORDER BY measured_at;
```

---
##  오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분:**
	- `LAG()` 사용 >이전 값 하나만 가져옴, 이동 평균은 여러 행 필요
	- measured_at + INTERVAL '10 minute' 이걸 생각지 못했었다.
	- 10분 단위 × 6개 = 1시간> → **ROWS 5 PRECEDING**이 정답
    
-  **새로 알게 된 함수/꿀팁:**

---
## 더 나은 풀이가 있다면?

```sql
-- 더 나은 풀이가 있을경우 
```
