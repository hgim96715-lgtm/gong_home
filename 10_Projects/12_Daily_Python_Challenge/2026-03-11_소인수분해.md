---
tags:
  - Python_Test
status: 🟨 힌트참고
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_Sets]]"
difficulty:
  - Lv.0(입문)
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 소인수분해란 어떤 수를 소수들의 곱으로 표현하는 것입니다. 
> 예를 들어 12를 소인수 분해하면 2 * 2 * 3 으로 나타낼 수 있습니다. 따라서 12의 소인수는 2와 3입니다. 
> 자연수 `n`이 매개변수로 주어질 때 `n`의 소인수를 오름차순으로 담은 배열을 return하도록 solution 함수를 완성해주세요.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: 자연수 `n`
    - **Output**: `n`의 소인수를 오름차순으로 담은 배열
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)
	- 같은 소수가 여러 번 나올 수 있으므로 **중복 제거를 위해 `set` 
	- `n % i == 0`이면 `i`는 **소인수**
3. **사용할 함수/라이브러리:**
   - 

---
## ✅ 정답 코드 (Solution)

```python
def solution(n):
    answer = set()
    i=2
    while n>1:
        if n%i==0:
            answer.add(i)
            n=n//i
        else:
            i+=1
    return sorted(answer)
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
	- 처음에는 `if`문으로 한 번만 나누려고 해서 **소인수분해의 반복 구조를 만들지 못했다.**
	- `while` 반복문을 **어떤 조건으로 돌려야 할지 몰라 구현이 막혔다.**
    
-  **새로 알게 된 함수/메서드:**
	- 소인수분해에서는 `n`이 계속 줄어들기 때문에 **`while n > 1` 조건으로 반복하면 된다**
	- 나눗셈을 검사할 **인덱스 변수 `i`를 따로 두고 (`i = 2`)**  나누어지면 `n //= i`, 나누어지지 않으면 `i += 1` 하는 패턴

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
```