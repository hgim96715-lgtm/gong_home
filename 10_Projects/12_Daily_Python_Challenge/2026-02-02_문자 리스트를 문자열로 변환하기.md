---
tags:
  - Python_Test
related:
  - "[[Python_String_Methods#반대 기술 `join()`]]"
  - "[[00_Python_Challenge_DashBoard]]"
difficulty:
  - Lv.0(기초)
source: programmers
---

## 문제 분석 & 설계 (Code Before Think)

>  문자들이 담겨있는 배열 `arr`가 주어집니다. 
>  `arr`의 원소들을 순서대로 이어 붙인 문자열을 return 하는 solution함수를 작성해 주세요.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
	- **input:** 문자들이 담긴 배열 `arr`
	- **output:**`arr`의 원소들을 **순서대로 이어 붙인 하나의 문자열**

2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)

3. **사용할 함수/라이브러리:**
   - 

---
## ✅ 정답 코드 (Solution)

```python
def solution(arr):
    answer = ''
    for v in arr:
        answer+=v
    return answer
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
    
-  **새로 알게 된 함수/메서드:**
		- `str.join(iterable)` : 내부적으로 최적화되어 있어 **성능이 훨씬 좋음**

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
def solution(arr):
    return ''.join(arr)
```

>> **문자 배열을 순서대로 문자열로 합치는 문제 → `join` 사용 