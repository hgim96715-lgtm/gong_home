---
tags:
  - Python_Test
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_String_Methods]]"
difficulty:
  - Lv.1
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 어떤 문장의 각 알파벳을 일정한 거리만큼 밀어서 다른 알파벳으로 바꾸는 암호화 방식을 시저 암호라고 합니다. 예를 들어 "AB"는 1만큼 밀면 "BC"가 되고, 3만큼 밀면 "DE"가 됩니다. "z"는 1만큼 밀면 "a"가 됩니다. 문자열 s와 거리 n을 입력받아 s를 n만큼 민 암호문을 만드는 함수, solution을 완성해 보세요.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: 문자열 `s` , 정수 `n`
    - **Output**: 문자열 `s`의 각 알파벳을 `n`칸씩 민 새로운 문자열
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)
	- 대문자라면 `'A'`를 기준
	- 소문자라면 `'a'`를 기준
3. **사용할 함수/라이브러리:**
	-   `ord()`
	- `chr()`

---
## ✅ 정답 코드 (Solution)

```python
def solution(s, n):
    answer = ''
    for s1 in s:
        if s1 == ' ':
            answer += ' '
        elif 'A' <= s1 <= 'Z':
            p = ord(s1) - ord('A')
            # print(p)
            new_p = (p + n) % 26
            # print(new_p)
            answer += chr(new_p + ord('A'))
        else:
            p=ord(s1)-ord('a')
            new_p=(p+n)%26
            answer+=chr(new_p+ord('a'))
    return answer
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
	- 처음에는 문자열 앞뒤 공백을 제거하려고 `strip()`을 사용했지만, 이 문제에서는 **공백도 문자열의 일부**로 유지
	- `answer += ord(s1) - ord('A')`는 안 된다.
-  **새로 알게 된 함수/메서드:**

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
def solution(s, n):
    answer = ''
    
    for ch in s:
        if ch == ' ':
            answer += ' '
        else:
            base = ord('A') if ch.isupper() else ord('a')
            answer += chr((ord(ch) - base + n) % 26 + base)
            
    return answer
```