---
tags:
  - Python_Test
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_Lists_Tuples]]"
difficulty:
  - Lv.1
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 자연수 n을 뒤집어 각 자리 숫자를 원소로 가지는 배열 형태로 리턴해주세요. 
> 예를들어 n이 12345이면 `[5,4,3,2,1]`을 리턴합니다.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: 자연수 n
    - **Output**:  배열형태 
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)

3. **사용할 함수/라이브러리:**
   - `[:​:-1]` **문자열/리스트를 뒤집는 슬라이싱**
   - `str(n)` 숫자를 문자열로 변환

---
## ✅ 정답 코드 (Solution)

```python
def solution(n):
    answer = [int(a) for a in str(n)[::-1]]
    return answer
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
	- 처음에 `sorted()`를 사용해서 문제를 풀려고 해서 문제의 순서를 유지한채 반대로 뒤집는 로직이랑 맞지 않아서 테스트 에서 몇개가 실패가  떴었다.
	- `sorted(..., reverse=True)` 가 아니라 `[:​:-1]` 을 하는 것!
-  **새로 알게 된 함수/메서드:**

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
def digit_reverse(n):
    return list(map(int, reversed(str(n))))
```