---
tags:
  - Python_Test
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_Lists_Tuples]]"
  - "[[Python_Looping_Helpers]]"
difficulty:
  - Lv.1
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 행렬의 덧셈은 행과 열의 크기가 같은 두 행렬의 같은 행, 같은 열의 값을 서로 더한 결과가 됩니다. 2개의 행렬 arr1과 arr2를 입력받아, 행렬 덧셈의 결과를 반환하는 함수, solution을 완성해주세요.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: `arr1`, `arr2` → 같은 크기의 2차원 리스트 (행렬)
    - **Output**: 행렬의 같은 위치 값을 더한 결과 2차원 리스트
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)
	- 각 위치에서 `arr1[i][j] + arr2[i][j]` 계산
	- 각 행마다 새로운 리스트를 만들어 결과에 추가
3. **사용할 함수/라이브러리:**
	-  `len()` → 행/열 길이 확인

---
## ✅ 정답 코드 (Solution)

```python
def solution(arr1, arr2):
    answer = []
    for i in range(len(arr1)):
        row=[]
        for j in range(len(arr1[0])):
            row.append(arr1[i][j]+arr2[i][j])
        answer.append(row)
    return answer
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
    
-  **새로 알게 된 함수/메서드:**
	- `zip()` → 같은 인덱스끼리 묶어줌
	- 리스트 컴프리헨션으로 이중 반복문을 한 줄로 표현 가능
	- 행렬 문제 = **이중 반복문 (행 + 열)**
	- “같은 위치” 처리 → 인덱스 or `zip`

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
def sumMatrix(A,B):
    answer = [[c + d for c, d in zip(a,b)] for a, b in zip(A,B)]
    return answer
```