---
tags:
  - Python_Test
status: 🟩 해결
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_Collections_Counter]]"
difficulty:
  - Lv.0(입문)
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 최빈값은 주어진 값 중에서 가장 자주 나오는 값을 의미합니다. 
> 정수 배열 `array`가 매개변수로 주어질 때, 최빈값을 return 하도록 solution 함수를 완성해보세요. 최빈값이 여러 개면 -1을 return 합니다.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: 정수로 이루어진 리스트 `array`
    - **Output**: 가장 많이 등장한 정수 값 (최빈값) , 단, 최빈값이 **2개 이상이면 `-1` 반환**
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)
	- 배열 내 각 숫자의 **등장 횟수(빈도)**를 계산한다.
3. **사용할 함수/라이브러리:**
   - `collections.Counter`
   - `max()`
---
## ✅ 정답 코드 (Solution)

```python
from collections import Counter

def solution(array):
    answer=0
    counter=Counter(array)
    counter_max=max(counter.values())
    for a,v in counter.items():
        if v==counter_max:
            answer+=1
            max_answer=a
    return max_answer if answer==1 else -1
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
	- 처음에 `Counter.values()`만 사용해 **빈도만 보고 값(최빈값)을 잃어버림**
    
-  **새로 알게 된 함수/메서드:**

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
from collections import Counter

def solution(array):
    a = Counter(array).most_common(2)
    if len(a) == 1:
        return a[0][0]
    if a[0][1] == a[1][1]:
        return -1
    return a[0][0]
```

>`Counter.most_common()`을 사용하면  **빈도 계산 + 정렬 + 비교**를 한 번에 처리할 수 있다.