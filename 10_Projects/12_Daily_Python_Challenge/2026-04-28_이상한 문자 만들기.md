---
tags:
  - Python_Test
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_String_Methods]]"
difficulty:
  - Lv.1
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 문자열 s는 한 개 이상의 단어로 구성되어 있습니다. 각 단어는 하나 이상의 공백문자로 구분되어 있습니다. 각 단어의 짝수번째 알파벳은 대문자로, 홀수번째 알파벳은 소문자로 바꾼 문자열을 리턴하는 함수, solution을 완성하세요.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: 하나 이상의 단어로 이루어짐
    - **Output**: 각 단어마다 인덱스를 새로 계산, 
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)
	- 문자열을 **한 글자씩 순회**
	- `idx = 0` 으로 초기화
	- `idx`가 짝수면 `upper()`
	- 홀수면 `lower()`
3. **사용할 함수/라이브러리:**
   - `str.upper()` : 대문자로 변환
   - `str.lower()` : 소문자로 변환

---
## ✅ 정답 코드 (Solution)

```python
def solution(s):
    answer = ''
    idx=0
    for i in s:
        if i ==' ':
            answer+=' '
            idx=0
        else:
            if idx%2:
                answer+=i.lower()
            else:
                answer+=i.upper()
            idx+=1
    return answer
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
    - 문자열 전체 인덱스로 계산함 >단어가 바뀌어도 인덱스가 초기화되지 않음
-  **새로 알게 된 함수/메서드:**
	- `upper()` / `lower()` 는 문자 1개에도 사용 가능
	- `split()` 과 `split(" ")` 은 다르게 동작함
	- `split(" ")` 을 사용해 공백 개수를 유지하면서 처리하는 방식

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
# 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?
def toWeirdCase(s):
    return " ".join(map(lambda x: "".join([a.lower() if i % 2 else a.upper() for i, a in enumerate(x)]), s.split(" ")))
```