---
tags:
  - Python_Test
status: 🟩 해결
related:
  - "[[00_Python_Challenge_DashBoard]]"
  - "[[Python_Regex]]"
  - "[[Python_String_Methods]]"
difficulty:
  - Lv.0(입문)
source: programmers
---
## 문제 분석 & 설계 (Code Before Think)

> 문자열 `my_string`이 매개변수로 주어집니다.
>  `my_string`은 소문자, 대문자, 자연수로만 구성되어있습니다. 
>  `my_string`안의 자연수들의 합을 return하도록 solution 함수를 완성해주세요.

1. **입/출력 분석:** (무엇을 받고, 무엇을 돌려주는가?)
    - **Input**: 소문자, 대문자, 숫자로 이루어진 문자열 (`str`)
    - **Output**: 문자열 안에 있는 **자연수들의 합** (`int`)
2. **핵심 로직:** (어떤 자료구조나 흐름을 쓸 것인가?)

3. **사용할 함수/라이브러리:**
   - `re.findall('[0-9]+', 문자열)` 

---
## ✅ 정답 코드 (Solution)

```python
import re
def solution(my_string):
    answer = 0
    nums=re.findall('[0-9]+', my_string)
    answer=sum([int(n) for n in nums])
    return answer
```

---
## 오답 노트 & 배운 점 (Retrospective)

-  **내가 실수한 부분 (Syntax/Logic):**
    
-  **새로 알게 된 함수/메서드:**
	- `str.isdigit()` 문자가 숫자인지 판별 (정규식 없이도 가능) 

>[[Python_String_Methods#⑩ isdigit() / isalpha() / isalnum() — 데이터 검증]] 참고 

---
## 더 나은 풀이 (Pythonic Code)

> 다른 사람의 풀이나, 더 간결한 코드(리팩토링)가 있다면?


```python
def solution(my_string):
    s = ''.join(i if i.isdigit() else ' ' for i in my_string)
    return sum(int(i) for i in s.split())
```